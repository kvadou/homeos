{
  "name": "homeos",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "db:generate": "pnpm --filter @homeos/database generate",
    "db:push": "pnpm --filter @homeos/database push",
    "db:migrate": "pnpm --filter @homeos/database migrate",
    "db:seed": "pnpm --filter @homeos/database seed",
    "db:studio": "pnpm --filter @homeos/database studio"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "esbuild",
      "@clerk/shared",
      "sharp"
    ]
  }
}
